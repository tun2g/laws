# ============================================================
# Root .env — copied/used by docker-compose
# Per-app .env files live in apps/*/.env (see each app's .env.example)
# ============================================================

# Postgres
POSTGRES_USER=laws
POSTGRES_PASSWORD=laws_dev_password
POSTGRES_DB=laws
POSTGRES_PORT=5432

# Redis (optional — only if you wire up BullMQ later)
REDIS_PORT=6379
